# Django Fruits

Ein einfaches Django-Projekt mit REST-Backend und separatem Frontend.

## Start
```bash
python -m venv venv
venv\Scripts\activate
pip install django django-cors-headers
python manage.py runserver
